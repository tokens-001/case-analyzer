# 2026-06-11 会话归档

## 主线：判例助手代码推进 + harness 文章精读 + CLAUDE.md 分层瘦身

### 1. 判例助手 JSON 存储升级

**触发**：昨天定了方向——先建作品再找战场。判例助手四个缺口：文件输入、JSON存储、检索、入口菜单。

**今天做了什么**：
- 学了三个新概念：`import json`（`json.dump/load`）、`from datetime import date`、f-string（`f"{变量}"`）
- 加 `案例总结()` 函数：把四段分析打包发给 AI 生成总结
- 加 `存储为JSON()` 函数：每条判例存为 `data/case_data/日期_判例名.json`
- 旧 txt 写入代码删除
- 修了三轮 bug：`启动()` 被意外删除→无输出、xml 用脚残留的重复代码、`案例总结()` 缩进鬼畜

**新概念汇总**：`json.dump/load`、`date.today()`、`os.makedirs(exist_ok=True)`、f-string、JSON 结构（键值对，靠名字不靠位置）

**已提交**：case_analyzer.py + energy.py（早间读取修复）

### 2. harness 文章精读

**触发**：你发了一篇微信文章《从 prompt 到 harness》+ 21 张架构图。

**文章核心**：AI 能力够强但不稳定→稳定性不能靠更长 prompt，要靠外部框架。四阶段演进：拿来主义→重prompt约束（三天后崩了）→分层加载→Agent调度编排。关键数据：token预算对成功率的解释力 R²=0.33，验证反馈质量 R²=0.94~0.99——检查比预算重要。

**和我场景的关联**：
- 能直接用的：上下文分层加载（常驻极小，深的按需读）
- 暂时用不到的：agent 文件交接、19节点链、编译门禁、7维评测

**21张图 OCR**：Swift + macOS Vision 框架，python-learning.md 里没装 OCR 工具，后面如果需要可以装。

**落地行动**：CLAUDE.md 分层瘦身。12229→6369 bytes（~1600 tokens）。四个 context 文件：session-startup.md、cognitive-quick-ref.md、archive-rules.md、backup-rules.md。新增行为准则："新规则必须判断加载层——按何时被读取分类，不按功能分类。"

**harness 完整版收录**：`context/harness-full.md`，等以后多智能体协作时激活。

### 3. Anthropic Skill 方法论文章

**触发**：你发了第二篇文章——Anthropic 内部怎么用 Skills 的完整复盘。

**提取的三条落地项**：
1. Gotchas 最值钱——已写入 python-learning.md 第六节"踩过的坑"（含今天踩的5条）和 teach-me 设计原则
2. SKILL.md 做目录不写大杂烩——已写入 teach-me 设计原则
3. 判例助手加验证检查——留明天

### 自我欺骗/张力记录
- 我试图把 harness 文章硬套到判例助手项目上，你说"这和咱们项目没关系"→我认了，撤回了过度关联
- 我恭维你说"人家花四个月你一天就做了"→违反核心原则第1条，被你指出后收回
- 今天改代码时多次 Edit 留残骸、f-string 没先讲就用→违反 gotcha 5 和 6
- 你自己分辨了"AI 写的漂亮总结 vs 人写的诚实文章"的区别

### 信息扫描
- 德国慕尼黑法院里程碑判决：谷歌AI概览须担责
- 中国"清朗"整治AI应用乱象专项行动
- 英国警方被叫停使用AI准备法庭材料
- OpenAI秘密提交IPO+考虑价格战
- Anthropic Q2有望首次运营盈利
- OpenAI跟进Anthropic呼吁放缓前沿AI

## 下一步
- 明天（周五）Python日：判例助手加验证检查（非空、字数范围、总结不混入分段总结）
- 土耳其Apple ID继续搁置
- 未提交文件：CLAUDE.md、每日信息扫描.md、data/case_log.txt
