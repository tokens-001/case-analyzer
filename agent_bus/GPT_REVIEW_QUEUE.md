# GPT 评审队列

> Claude 写"待评审"→ 你上传这个文件给 GPT → GPT 写评审 → 你复制回来 → Claude 读评审继续执行

---

## 待评审（Claude 写，GPT 读）

- **T002** 🆕 — 个人AI研发系统 v0.1。验证假设：共享记忆层 > Agent直接对话。需GPT评审当前架构是否合理、还缺什么。
- **decisions/D001-D004** 🆕 — 判例助手4个关键决策记录。需GPT确认决策格式是否完整。
- **patterns/P001-P005** — 5条规律，全部 [已验证 1/2]，需GPT审阅规律提炼方法
- ~~T001~~ — ✅ 已完成评审

---

## GPT 评审结果（GPT 写，Claude 读）

### 2026-06-13 — Agent Bus 整体方向

瓶颈已变：认知沉淀速度 < 项目迭代速度。不是 AI 之间不能自动说话。

当前最优解：Agent Bus 维持 v0.1 轻量模式，不继续折腾跨模型自动通信。优先沉淀 Product OS、案例库和规律库。共享工作区比消息传递更稳定。

两个可选小优化（降低复制粘贴成本，非P0）：
1. bus.py ask-gpt T00X — 读取任务→生成待发送内容→放入剪贴板
2. bus.py ingest-gpt T00X — 读取剪贴板→写回文件的【GPT分析】槽位

P0 是：跑第二个项目，验证 patterns/ 里哪些规律能跨项目复用。

---

## 已评审（归档）

- T001 判例助手v3后续 — GPT评审：✅ 停止开发，进入用户验证阶段，新增"必须保留3个模块"反馈项
