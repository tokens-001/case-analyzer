# Python学习案卷

## 第一节：第一句话
- `print("内容")` — 把括号里的东西输出到屏幕
- `"内容"` 叫字符串，用引号包起来

## 第二节：让程序问问题
- `input("提示文字")` — 输出提示，停下来等人输入，返回输入的内容
- 变量 — 把数据存起来，比如 `name = input("你是谁？")`，之后用 `name` 就能拿到输入的内容
- `+` — 把字符串拼在一起

## 第三节：判断
- `if 条件:` — 如果条件成立，执行下面缩进的代码
- `elif 条件:` — 否则如果另一个条件成立
- `else:` — 以上都不成立时执行
- `==` — 判断两边是否相等（一个 `=` 是赋值，两个 `==` 才是比较）
- **缩进必须一致**，Python 靠缩进识别代码块
- ✅ 已实践：guess.py 三次测试通过

## 第三节实践补充：input()
- `name = input("提示")` — 把用户输入的内容存到变量 name 里
- `print("你好，" + name)` — 用 + 拼接字符串和变量
- ✅ 已实践：ask.py 测试通过

## 第四节：循环
- `while 条件:` — 条件成立就一直重复执行缩进的代码块
- 循环变量需要在 while 之前初始化（如 `count = 1`）
- 循环体内必须改变条件变量（如 `count = count + 1`），否则死循环
- ✅ 已实践：loop.py 测试通过

## 学习路线（8天）

每天流程：复习旧语法10分钟 → 学新概念20分钟 → 写代码练习10分钟

- [ ] 第1天：for循环 + range() — `for i in range(5):`
- [ ] 第2天：列表基础 — 创建、索引、切片
- [ ] 第3天：列表操作 — append、remove、遍历列表
- [ ] 第4天：函数(def) — 定义函数、调用函数
- [ ] 第5天：函数进阶 — 参数、return返回值
- [ ] 第6天：字典(dict) — 键值对、增删改查
- [ ] 第7天：文件读写 — open、read、write
- [ ] 第8天：综合项目 — 乘法口诀表从零重写

## 第五节：for循环（待学习）
## 第六节：列表（待学习）
## 第七节：函数（待学习）
## 第八节：字典（待学习）
## 第九节：文件操作（待学习）
