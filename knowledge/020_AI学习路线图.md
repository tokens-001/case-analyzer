# 020 — AI 学习路线图

> 每个概念：一句话 → 你项目里对标什么 → 要不要现在学

---

## 第一层：已经在用的（9项，不需要学）

| 概念 | 一句话 | 你项目里的位置 |
|------|--------|-------------|
| Skill | 独立的能力模块，输入→输出 | `skills/legal/` 13 个 .py 文件 |
| Workflow | 多个技能的执行顺序 | `/analyze` 四轮调度 |
| Orchestration | 管理和调度多技能执行 | `ThreadPoolExecutor` 并行调度 |
| Validation Layer | AI 生成后用非 AI 方式校验 | `verify_laws.py + trace_citations.py + score_analysis.py` |
| Context Engineering | 精心设计提示词让 AI 输出更好 | 法条提示变量 + `[第X段]` 编号标记 |
| Knowledge Management | 把发现/决策/规律结构化存下来 | `learnings/ → experiments/ → patterns/ → decisions/ → open_questions/` |
| Learning Loop | 用户反馈 → 提炼规律 → 验证 → 沉淀 | 反馈系统 6 步选择题 → `experiments/` → `patterns/` |
| Agent Bus | 多个 Agent 通过共享文件通信 | `agent_bus/bus.py` + `tasks/*.md` + 状态机 |
| Multi-Agent Workflow | 多个不同专长的 AI 协作完成复杂任务 | GPT（分析）→ Claude（执行）→ GPT（评审）→ 你（决策） |

---

## 第二层：原理知道但没亲手实现（4项，暂不学）

### RAG — 检索增强生成

**一句话**：先查外部资料，把结果塞进提示词，让 AI 参考着回答。不是靠 AI 的记忆。

**你项目的对标**：`search_law_database()` 在法条库里查到 AI 引用的法条原文 → 展示给人对照。你走的是"事后校验"路线（AI 生成后对答案），传统 RAG 走"事前增强"路线（AI 生成前喂资料）。方向不同，目的相同。

**什么时候学**：法条库大到文件名匹配漏东西，或者想自动把法条原文注回 AI 提示词时。

---

### Agent — 智能体

**一句话**：能自己决定"下一步做什么"的 AI。不是被动回答问题，是主动规划步骤。

**你项目的对标**：`/analyze` 四轮流程是预设好的。真正的 Agent：你告诉它"分析判例"，它自己决定先调哪个 skill、后调哪个，每一步是 AI 选的不是代码定的。

**为什么现在不需要**：法律场景需要确定性。验证层每一步都不能跳过，Agent 可能省成本省略校验。

**什么时候学**：有 50+ 个技能、用户需求各异，没法预设所有流程时。

---

### Tool Calling — 工具调用

**一句话**：AI 能调用外部函数（查数据库、发请求），不只是生成文字。

**你项目的对标**：你的代码决定何时调 `search_law_database()`。Tool Calling 反过来：AI 说"我需要查法条库了" → 自动调函数 → 拿到结果继续分析。

**为什么现在不需要**：校验层顺序是固定的，不需要 AI 自己决定。

**什么时候学**：技能多到没法预设流程，需要 AI 自己判断该调哪个工具时。

---

### MCP — 模型上下文协议

**一句话**：一套标准接口，不管什么模型（GPT/Claude/DeepSeek），用同一种方式接入外部工具。

**你项目的对标**：只接了 DeepSeek 一个模型，工具都是直接 import Python 函数。MCP 的场景是 GPT + Claude + DeepSeek 三个模型都想调 `skills/legal/`，不用写三套接口。

**什么时候学**：有 2+ 个模型且共享同一套技能库时。

---

## 第三层：需要学的（5项，按优先级排列）

### P1 — Embedding / 向量嵌入

**一句话**：把一段文字变成一串数字（向量）。相似内容的向量也相似。

**对比你现在**：
```
现在（关键词）: if "民法典" in 文件名 → 找到
以后（向量）:   用户搜"劳动纠纷" → 找到写"解除劳动合同"的判例
```

**什么时候该学**：法条库超 50 部法律，或判例超 100 份，文件名匹配开始漏东西。当前量不需要。学的时候先了解 `text-embedding-3-small` 这个 API。

---

### P2 — Semantic Search / 语义搜索

**一句话**：Embedding 是技术，Semantic Search 是应用。= Embedding + 检索 + 排序。

**对比你现在**：`过滤历史()` 用 `.includes(kw)` 匹配。搜"解雇"找不到"解除劳动合同"。

**什么时候该学**：需要给判例助手加"语义搜索历史判例"功能时。需要向量数据库（Chroma 最简单，一个 pip install）。

---

### P3 — Fine-tuning / 微调

**一句话**：拿 100 份标注数据再训练模型，让它在你领域更准。

**对比你现在**：提示词里写"【硬性要求】必须引用法条"（Prompt Engineering）。微调是让模型自己学会引用法条。

**什么时候微调 > 提示词**：同一个错误提示词改到第十版还控制不住；或者你有 100+ 份高质量标注数据。目前不需要——提示词 + 验证层已经兜底了。

---

### P4 — LangChain / LangGraph

**一句话**：LangChain = LLM 调用链。LangGraph = 状态图编排。都是"把多个 AI 调用串成流程"的框架。

**对比你现在**：
```python
# 你的手写版（灵活、精确、零依赖）
with ThreadPoolExecutor() as executor:
    f1 = executor.submit(extract_dispute.执行, ...)
   分析1 = f1.result()
   总结 = generate_summary.执行(..., 分析1, ...)

# LangChain版（多了重试/缓存/日志，但加了抽象层）
chain = extract_dispute | generate_summary
result = chain.invoke(判例)
```

**什么时候该学**：手写 `ThreadPoolExecutor` 管理到 10+ 步骤、有分支逻辑（校验失败→重试 AI）、有错误恢复需求时。当前不需要——4 轮调度不复杂。

---

### P5 — CrewAI / AutoGen

**一句话**：多 Agent 编排框架。定义角色、任务、执行顺序。

**对比你现在**：`agent_bus/` 就是手写版——三个角色通过共享文件通信。CrewAI 用代码驱动自动流转。

**什么时候该学**：角色多到 agent_bus 手写不过来（5+ 个 Agent，10+ 个任务类型）。当前 3 个角色足够。

---

## 第四层：完全没概念的（4项，碰到了再学）

| 概念 | 一句话 | 触发条件 |
|------|--------|---------|
| Streaming / SSE | AI 一个字一个字往外蹦，不用等 35 秒 | 用户嫌等太久 |
| Token 预算管理 | 控制输入+输出不超过模型上下文窗口 | 判例文字超 4 万字（DeepSeek 64K token） |
| Evaluation | 量化评估"AI 分析好不好"（不是事实性校验，是有用性评估） | 想知道 AI 分析质量是不是在改善 |
| Guardrails | 限制 AI 不乱说（敏感信息过滤、伦理边界） | 判例包含个人隐私信息 |

---

## 学习优先级总结

```
现在不需要动的：  9项 ✅  + 4项 🟡  = 13项（已经在项目里用着或知道原理）
下一个该学的：    P1 Embedding → P2 Semantic Search（这俩是一套）
再下一个：        P3 Fine-tuning → P4 LangChain → P5 CrewAI
碰到了再学：      4项 ⬜（Streaming / Token / Eval / Guardrails）
```

---

## 判断规则

以后听到任何新概念，顺序问自己三句：

1. **它管什么层？**（Skill / Workflow / Knowledge / Model / 都不属于）
2. **跟我项目里哪个东西对标？**（答不上来 = 真盲区）
3. **我现在的项目碰到它要解决的问题了吗？**（没碰到 = 不现在学）
